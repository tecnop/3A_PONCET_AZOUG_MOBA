using UnityEngine;
using System.Collections;

public static class MetadataDisplayer
{
	static void Display(Metadata data, Rect frame)
	{
		/*float w = frame.width;
		float h = frame.height;
		Rect localRect = new Rect(0.0f, 0.0f, w, h);

		GUI.BeginGroup(frame);

		string iconPath = data.GetIcon();

		if (iconPath != null)
		{ // TODO: Icon

		}

		Quality quality = data.GetQuality();
		// TODO: Style depending on quality

		string name = data.GetName();

		string desc = data.GetDesc();

		GUI.EndGroup();*/
	}
}
